<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sam.cheezycodedemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CheezyCodeDemo">
        <activity
            android:name=".bindingadapter.BindingAdapterActivity"
            android:exported="true" />
        <activity
            android:name=".databindingwithlivedata.DataBindingLiveDataActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".databinding.DataBindingMainActivity"
            android:exported="true"></activity>
        <activity
            android:name=".livedata.MainActivity"
            android:exported="true" />
    </application>

</manifest>